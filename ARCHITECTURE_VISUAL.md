# 🏗️ AkasaEats Backend Architecture

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          AKASA EATS PLATFORM                                 │
│                     (Microservices Architecture)                             │
└─────────────────────────────────────────────────────────────────────────────┘

                              ┌──────────────────┐
                              │   React Client   │
                              │   (Frontend)     │
                              │   Port 3000      │
                              └────────┬─────────┘
                                       │
                                       │ HTTP/REST
                                       │
         ┌─────────────────────────────┼─────────────────────────────┐
         │                             │                             │
         ▼                             ▼                             ▼
┌──────────────────┐         ┌──────────────────┐         ┌──────────────────┐
│  Auth Service    │         │  Item Inventory  │         │  Cart Service    │
│   Port 3001      │         │   Port 3002      │         │   Port 3003      │
│                  │         │                  │         │                  │
│ ┌──────────────┐ │         │ ┌──────────────┐ │         │ ┌──────────────┐ │
│ │ Express.js   │ │         │ │ Express.js   │ │         │ │ Express.js   │ │
│ │    Router    │ │         │ │    Router    │ │         │ │    Router    │ │
│ └──────┬───────┘ │         │ └──────┬───────┘ │         │ └──────┬───────┘ │
│        │         │         │        │         │         │        │         │
│ ┌──────▼───────┐ │         │ ┌──────▼───────┐ │         │ ┌──────▼───────┐ │
│ │  Middleware  │ │         │ │  Middleware  │ │         │ │  Middleware  │ │
│ │  - CORS      │ │         │ │  - CORS      │ │         │ │  - CORS      │ │
│ │  - Body      │ │         │ │  - Body      │ │         │ │  - Body      │ │
│ │  - Morgan    │ │         │ │  - Morgan    │ │         │ │  - Morgan    │ │
│ │  - JWT Auth  │ │         │ │  - JWT Auth  │ │         │ │  - JWT Auth  │ │
│ │  - Error     │ │         │ │  - Multer    │ │         │ │  - Error     │ │
│ └──────┬───────┘ │         │ │  - Error     │ │         │ └──────┬───────┘ │
│        │         │         │ └──────┬───────┘ │         │        │         │
│ ┌──────▼───────┐ │         │ ┌──────▼───────┐ │         │ ┌──────▼───────┐ │
│ │ Controllers  │ │         │ │ Controllers  │ │         │ │ Controllers  │ │
│ │  - register  │ │         │ │  - getAllIt  │ │         │ │  - getCart   │ │
│ │  - login     │ │         │ │  - createIt  │ │         │ │  - addToCart │ │
│ │  - profile   │ │         │ │  - updateIt  │ │         │ │  - updateQty │ │
│ │  - logout    │ │         │ │  - deleteIt  │ │         │ │  - remove    │ │
│ │  - tokens    │ │         │ │  - stock     │ │         │ │  - clear     │ │
│ └──────┬───────┘ │         │ │  - search    │ │         │ │  - validate  │ │
│        │         │         │ └──────┬───────┘ │         │ └──────┬───────┘ │
│        │         │         │        │         │         │        │         │
│        │         │         │ ┌──────▼───────┐ │         │ ┌──────▼───────┐ │
│        │         │         │ │   Services   │ │         │ │   Services   │ │
│        │         │         │ │  - Upload    │ │         │ │  - itemSvc   │ │
│        │         │         │ │    Handler   │ │         │ │    (Axios)   │ │
│        │         │         │ └──────────────┘ │         │ └──────┬───────┘ │
│        │         │         │                  │         │        │         │
└────────┼─────────┘         └────────┼─────────┘         └────────┼─────────┘
         │                            │                            │
         │                            │                            │
         │                            │                ┌───────────┘
         │                            │                │
         │                            │                │ HTTP Call
         │                            │                │ (Token Forward)
         │                            │                │
         │                            │                ▼
         │                            │      ┌──────────────────┐
         │                            │      │  Item Service    │
         │                            │      │  (Port 3002)     │
         │                            │      │  GET/POST Items  │
         │                            │      └──────────────────┘
         │                            │
         └────────────┬───────────────┴───────────────┐
                      │                               │
                      ▼                               ▼
         ┌────────────────────────┐      ┌────────────────────────┐
         │   Firebase Auth        │      │  Firebase Storage      │
         │   - JWT Token Gen      │      │  - Image Upload        │
         │   - User Management    │      │  - Image Retrieval     │
         │   - Token Verify       │      │  - URL Generation      │
         └────────────────────────┘      └────────────────────────┘
                      │
                      │
                      ▼
         ┌─────────────────────────────────────────────┐
         │         Cloud Firestore                      │
         │                                              │
         │  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
         │  │  users/  │  │  items/  │  │  carts/  │  │
         │  │  {uid}   │  │  {id}    │  │  {uid}   │  │
         │  └──────────┘  └──────────┘  └──────────┘  │
         └─────────────────────────────────────────────┘
```

---

## Data Flow Diagrams

### 1. User Registration Flow

```
┌──────────┐       ┌──────────────┐       ┌──────────────┐       ┌──────────┐
│  Client  │       │ Auth Service │       │Firebase Auth │       │Firestore │
└────┬─────┘       └──────┬───────┘       └──────┬───────┘       └────┬─────┘
     │                    │                      │                     │
     │ POST /register     │                      │                     │
     │ {email,password}   │                      │                     │
     ├───────────────────>│                      │                     │
     │                    │                      │                     │
     │                    │ createUser()         │                     │
     │                    ├─────────────────────>│                     │
     │                    │                      │                     │
     │                    │      User Created    │                     │
     │                    │<─────────────────────┤                     │
     │                    │                      │                     │
     │                    │ createCustomToken()  │                     │
     │                    ├─────────────────────>│                     │
     │                    │                      │                     │
     │                    │      JWT Token       │                     │
     │                    │<─────────────────────┤                     │
     │                    │                      │                     │
     │                    │           Store user data                  │
     │                    ├───────────────────────────────────────────>│
     │                    │                      │                     │
     │                    │                      │        Success      │
     │                    │<───────────────────────────────────────────┤
     │                    │                      │                     │
     │  Response:         │                      │                     │
     │  {user,token}      │                      │                     │
     │<───────────────────┤                      │                     │
     │                    │                      │                     │
```

### 2. Add to Cart Flow (with Stock Validation)

```
┌──────────┐   ┌────────────┐   ┌────────────┐   ┌────────────┐   ┌──────────┐
│  Client  │   │Cart Service│   │Item Service│   │   Items    │   │Firestore │
└────┬─────┘   └─────┬──────┘   └─────┬──────┘   └─────┬──────┘   └────┬─────┘
     │               │                │                │                │
     │ POST /cart    │                │                │                │
     │ {itemId,qty}  │                │                │                │
     │ + JWT Token   │                │                │                │
     ├──────────────>│                │                │                │
     │               │                │                │                │
     │               │ Verify Token   │                │                │
     │               │ Extract userId │                │                │
     │               │                │                │                │
     │               │ GET /items/:id │                │                │
     │               │ + Forward Token│                │                │
     │               ├───────────────>│                │                │
     │               │                │                │                │
     │               │                │  Get Item Data │                │
     │               │                ├───────────────>│                │
     │               │                │                │                │
     │               │                │   Item Details │                │
     │               │                │<───────────────┤                │
     │               │                │                │                │
     │               │  Item Details  │                │                │
     │               │<───────────────┤                │                │
     │               │                │                │                │
     │               │ POST check     │                │                │
     │               │ stock {qty}    │                │                │
     │               ├───────────────>│                │                │
     │               │                │                │                │
     │               │                │  Verify Stock  │                │
     │               │                ├───────────────>│                │
     │               │                │                │                │
     │               │                │ Stock OK:true  │                │
     │               │                │<───────────────┤                │
     │               │                │                │                │
     │               │   Stock OK     │                │                │
     │               │<───────────────┤                │                │
     │               │                │                │                │
     │               │         Update/Create cart                       │
     │               ├────────────────────────────────────────────────>│
     │               │                │                │                │
     │               │                │                │   Cart Updated │
     │               │<────────────────────────────────────────────────┤
     │               │                │                │                │
     │  Cart Data    │                │                │                │
     │<──────────────┤                │                │                │
     │               │                │                │                │
```

### 3. Get Cart with Enriched Data Flow

```
┌──────────┐   ┌────────────┐   ┌────────────┐   ┌──────────┐
│  Client  │   │Cart Service│   │Item Service│   │Firestore │
└────┬─────┘   └─────┬──────┘   └─────┬──────┘   └────┬─────┘
     │               │                │                │
     │ GET /cart     │                │                │
     │ + JWT Token   │                │                │
     ├──────────────>│                │                │
     │               │                │                │
     │               │ Verify Token   │                │
     │               │                │                │
     │               │       Get cart from Firestore   │
     │               ├────────────────────────────────>│
     │               │                │                │
     │               │    Cart: [{itemId,qty}]         │
     │               │<────────────────────────────────┤
     │               │                │                │
     │               │ For each item: │                │
     │               │ GET /items/:id │                │
     │               ├───────────────>│                │
     │               │                │                │
     │               │  {name,price,  │                │
     │               │   images,...}  │                │
     │               │<───────────────┤                │
     │               │                │                │
     │               │ Enrich data:   │                │
     │               │ - Add name     │                │
     │               │ - Add price    │                │
     │               │ - Add images   │                │
     │               │ - Calc subtotal│                │
     │               │                │                │
     │               │ Calculate:     │                │
     │               │ - Total items  │                │
     │               │ - Total price  │                │
     │               │                │                │
     │  Enriched     │                │                │
     │  Cart Data    │                │                │
     │<──────────────┤                │                │
     │               │                │                │
```

---

## Service Communication Patterns

### 1. Token Forwarding Pattern

```
Client
  │
  │ Authorization: Bearer <token>
  ▼
Cart Service (Port 3003)
  │
  │ 1. Verify token locally
  │ 2. Extract user info
  │
  │ Authorization: Bearer <same-token>
  ▼
Item Service (Port 3002)
  │
  │ 1. Verify token again
  │ 2. Authorize request
  │ 3. Return data
  ▼
Response flows back to client
```

### 2. User Isolation Pattern

```
Request with JWT Token
        │
        ▼
    Extract userId from token
        │
        ▼
    Use userId as document ID
        │
        ├──> carts/{userId}     (Cart Service)
        ├──> users/{userId}     (Auth Service)
        └──> orders/{userId}    (Future: Order Service)
        
Result: User can ONLY access their own data
```

### 3. Stock Validation Pattern

```
Cart Operation Request
        │
        ▼
    ┌──────────────────┐
    │  Cart Service    │
    └────────┬─────────┘
             │
             │ 1. GET item details
             ▼
    ┌──────────────────┐
    │  Item Service    │
    │  - Verify exists │
    └────────┬─────────┘
             │
             │ 2. Check stock
             ▼
    ┌──────────────────┐
    │  Item Service    │
    │  - Stock >= qty? │
    └────────┬─────────┘
             │
             ├── Yes ──> Allow operation
             │
             └── No ───> Reject with error
```

---

## Security Architecture

```
┌─────────────────────────────────────────────────────┐
│              Security Layers                        │
└─────────────────────────────────────────────────────┘

Layer 1: CORS Protection
├─ Whitelist allowed origins
├─ Block unauthorized domains
└─ Credentials: true

Layer 2: JWT Authentication
├─ Firebase ID Token verification
├─ Token expiration (1 hour)
├─ User identity extraction
└─ Token revocation support

Layer 3: User Isolation
├─ Document-level security (userId as doc ID)
├─ Middleware validation (req.user check)
└─ Query filtering by userId

Layer 4: Input Validation
├─ Required field checks
├─ Type validation
├─ Range validation (qty > 0)
└─ Sanitization

Layer 5: Service Authorization
├─ Token forwarding between services
├─ Re-verification at each service
└─ Consistent authorization logic

Layer 6: Error Sanitization
├─ No sensitive data in errors
├─ Generic error messages for security issues
└─ Detailed logs server-side only
```

---

## Deployment Architecture (Future)

```
┌─────────────────────────────────────────────────────────────┐
│                    Cloud Infrastructure                      │
└─────────────────────────────────────────────────────────────┘

                    ┌──────────────────┐
                    │  Load Balancer   │
                    │  (Cloud LB)      │
                    └────────┬─────────┘
                             │
         ┌───────────────────┼───────────────────┐
         │                   │                   │
         ▼                   ▼                   ▼
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│ Auth Service │    │ Item Service │    │ Cart Service │
│  Container   │    │  Container   │    │  Container   │
│  (x3 pods)   │    │  (x3 pods)   │    │  (x3 pods)   │
└──────────────┘    └──────────────┘    └──────────────┘
         │                   │                   │
         └───────────────────┼───────────────────┘
                             │
                    ┌────────▼─────────┐
                    │  Firebase Cloud  │
                    │  - Firestore     │
                    │  - Auth          │
                    │  - Storage       │
                    └──────────────────┘

Monitoring & Logging:
├─ Cloud Logging
├─ Cloud Monitoring
├─ Error Reporting
└─ Trace
```

---

## Database Relationships

```
┌─────────────────────────────────────────────────────────────┐
│                  Firestore Collections                       │
└─────────────────────────────────────────────────────────────┘

users/{userId}
    ├─ email
    ├─ displayName
    ├─ photoURL
    └─ metadata
         │
         │ Referenced by
         ▼
    carts/{userId}  ◄───── Same userId (1-to-1)
         │
         │ Contains
         ▼
    items: [
         {
           itemId ───────────┐
           quantity          │
           addedAt           │
         }                   │
    ]                        │
                             │ References
                             ▼
                        items/{itemId}
                             ├─ name
                             ├─ price
                             ├─ stockQuantity
                             ├─ category
                             └─ images[]

Future:
    orders/{orderId}
         ├─ userId ──────────> users/{userId}
         └─ items: [{
              itemId ────────> items/{itemId}
              quantity
              price (snapshot)
           }]
```

---

## Request/Response Flow Chart

```
┌──────────────────────────────────────────────────────────────┐
│                   Typical API Request                         │
└──────────────────────────────────────────────────────────────┘

1. Client Request
   ↓
2. CORS Middleware
   ├─ Check origin
   └─ Set headers
   ↓
3. Body Parser
   ├─ Parse JSON
   └─ Populate req.body
   ↓
4. Morgan Logger
   └─ Log request
   ↓
5. Route Matching
   └─ Find handler
   ↓
6. Verify Token Middleware
   ├─ Extract token
   ├─ Verify with Firebase
   ├─ Extract user
   └─ Attach to req.user
   ↓
7. Controller Function
   ├─ Validate input
   ├─ Business logic
   ├─ Call other services (if needed)
   └─ Database operations
   ↓
8. Response
   ├─ Format JSON
   └─ Set status code
   ↓
9. Error Handler (if error occurs)
   ├─ Catch error
   ├─ Format error response
   └─ Log error
   ↓
10. Send to Client
```

---

## Technology Stack Layers

```
┌─────────────────────────────────────────────────────────┐
│                   Frontend Layer                         │
│              React.js (To be built)                      │
└─────────────────────────────────────────────────────────┘
                          │
                          │ HTTP/REST APIs
                          ▼
┌─────────────────────────────────────────────────────────┐
│                  API Gateway Layer                       │
│         (Future: API Gateway / Load Balancer)           │
└─────────────────────────────────────────────────────────┘
                          │
        ┌─────────────────┼─────────────────┐
        │                 │                 │
        ▼                 ▼                 ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│ Application  │  │ Application  │  │ Application  │
│    Layer     │  │    Layer     │  │    Layer     │
│              │  │              │  │              │
│ Node.js      │  │ Node.js      │  │ Node.js      │
│ Express.js   │  │ Express.js   │  │ Express.js   │
└──────────────┘  └──────────────┘  └──────────────┘
        │                 │                 │
        └─────────────────┼─────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│              Firebase Platform Layer                     │
│                                                          │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐        │
│  │ Firestore  │  │    Auth    │  │  Storage   │        │
│  │   (DB)     │  │   (JWT)    │  │  (Images)  │        │
│  └────────────┘  └────────────┘  └────────────┘        │
└─────────────────────────────────────────────────────────┘
```

---

## Scalability Considerations

### Horizontal Scaling

```
Single Instance              Load Balanced (Future)
┌────────────┐              ┌────────────────────────┐
│ Service    │              │   Load Balancer        │
│ Port 3001  │              └──────┬─────────────────┘
└────────────┘                     │
                    ┌──────────────┼──────────────┐
                    ▼              ▼              ▼
              ┌─────────┐    ┌─────────┐    ┌─────────┐
              │Instance1│    │Instance2│    │Instance3│
              │Port 3001│    │Port 3001│    │Port 3001│
              └─────────┘    └─────────┘    └─────────┘
                    │              │              │
                    └──────────────┼──────────────┘
                                   │
                          ┌────────▼────────┐
                          │   Firestore     │
                          │  (Scales Auto)  │
                          └─────────────────┘
```

### Caching Strategy (Future)

```
Request
   │
   ▼
┌─────────────┐
│ Redis Cache │  <── Cache frequently accessed items
└──────┬──────┘      (product details, categories)
       │
   Cache Miss
       │
       ▼
┌─────────────┐
│  Firestore  │  <── Database query
└─────────────┘
       │
       │ Update cache
       └──────> Cache
```

---

## Error Handling Flow

```
Request → Service
              │
              ▼
        Try/Catch Block
              │
         ┌────┴────┐
         │         │
    Success      Error
         │         │
         │         ▼
         │    Error Type?
         │         │
         │    ┌────┴────┬────────┬──────────┐
         │    │         │        │          │
         │  Client  Service  Database   Unknown
         │   Error   Error    Error      Error
         │   (400)   (503)    (500)      (500)
         │    │         │        │          │
         │    └─────────┴────────┴──────────┘
         │              │
         │              ▼
         │      Format Error Response
         │              │
         │              ▼
         │         Log Error
         │              │
         └──────────────┴───────> Send Response
```

---

**Architecture Status**: ✅ Documented & Implemented  
**Services Status**: ✅ All 3 services operational  
**Integration**: ✅ Fully tested  
**Scalability**: 🔄 Ready for enhancement

